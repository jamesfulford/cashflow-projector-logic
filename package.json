{
  "scripts": {
    "bundle": "npx esbuild src/sheets/functions.ts --bundle --global-name=logic --outfile=dist/bundle.js --format=iife",
    "test": "jest"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "esbuild": "^0.24.0",
    "lodash": "^4.17.21",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "jest": "^27.0.0",
    "ts-jest": "^27.0.0",
    "typescript": "^4.0.0"
  }
}
